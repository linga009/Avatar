# MED-RAI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a simulator-agnostic surgical robotic AI that takes endoscopic video, SE(3) kinematics, force/torque, and surgeon speech as input and outputs geodesic SE(3) trajectories, risk alerts, and gesture predictions.

**Architecture:** A Cross-Modal Alignment layer fuses four input modalities before a 4-bit NF4 QLoRA Jamba hybrid backbone. Three output heads branch from the backbone: a Riemannian Flow Matching Policy Head (SE(3) trajectories), a Text Head (risk alerts / surgical logs), and a Gesture Classifier (JIGSAWS 15-class vocabulary).

**Tech Stack:** PyTorch 2.x, PyTorch Lightning, HuggingFace Transformers + PEFT, bitsandbytes, pytorch3d, pytest

---

## File Structure

```
med_rai/
├── utils/
│   ├── __init__.py
│   └── se3_utils.py              # log/exp maps, workspace bounds, geodesic distance
├── encoders/
│   ├── __init__.py
│   ├── vision_encoder.py         # CLIP ViT-L/14 wrapper + linear probe
│   ├── se3_encoder.py            # SE(3) log-map + MLP encoder
│   ├── ft_encoder.py             # Force/torque MLP encoder
│   └── language_embedder.py      # Jamba tokenizer wrapper
├── fusion/
│   ├── __init__.py
│   └── cross_modal_alignment.py  # Cross-attention fusion across modalities
├── backbone/
│   ├── __init__.py
│   └── jamba_backbone.py         # Jamba QLoRA wrapper, returns hidden states
├── heads/
│   ├── __init__.py
│   ├── rfm_policy_head.py        # Riemannian Flow Matching DiT on SE(3)
│   ├── text_head.py              # LM head for risk alerts + surgical logs
│   └── gesture_head.py           # Linear classifier over 15 JIGSAWS gestures
├── loss.py                       # Weighted multi-task loss
├── model.py                      # MedRAI LightningModule (assembles all above)
├── data/
│   ├── __init__.py
│   ├── jigsaws_dataset.py        # JIGSAWS kinematic + video + gesture loader
│   ├── synthetic_se3.py          # Synthetic SE(3) geodesic trajectory generator
│   └── collate.py                # Multi-modal batch collation
├── training/
│   ├── __init__.py
│   ├── phase1_train.py           # Phase 1: encoder + alignment warmup
│   ├── phase2_train.py           # Phase 2: RFM head warmup
│   └── phase3_train.py           # Phase 3: joint fine-tuning
└── tests/
    ├── test_se3_utils.py
    ├── test_encoders.py
    ├── test_fusion.py
    ├── test_backbone.py
    ├── test_heads.py
    ├── test_loss.py
    ├── test_model.py
    └── test_integration.py
```

**Key constants (used throughout):**
- `D_JAMBA = 4096` — Jamba hidden size
- `D_HIDDEN = 512` — RFM head internal dim
- `SE3_DIM = 6` — se(3) Lie algebra dimension
- `TRAJ_HORIZON = 10` — trajectory steps (1s at 10Hz)
- `N_GESTURES = 15` — JIGSAWS gesture vocabulary
- `IMG_SIZE = 224` — ViT input resolution
- `FT_DIM = 6` — force/torque input (3 force + 3 torque)

---

## Task 1: Project Setup

**Files:**
- Create: `requirements.txt`
- Create: `pytest.ini`
- Create: `med_rai/__init__.py` and all `__init__.py` files

- [ ] **Step 1: Create requirements.txt**

```
torch>=2.1.0
torchvision>=0.16.0
pytorch-lightning>=2.1.0
transformers>=4.40.0
peft>=0.10.0
bitsandbytes>=0.43.0
pytorch3d
open-clip-torch>=2.24.0
numpy>=1.24.0
pandas>=2.0.0
pytest>=7.4.0
wandb
einops
```

- [ ] **Step 2: Create pytest.ini**

```ini
[pytest]
testpaths = med_rai/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
```

- [ ] **Step 3: Create all __init__.py files**

```bash
mkdir -p med_rai/{utils,encoders,fusion,backbone,heads,data,training,tests}
touch med_rai/__init__.py
touch med_rai/utils/__init__.py
touch med_rai/encoders/__init__.py
touch med_rai/fusion/__init__.py
touch med_rai/backbone/__init__.py
touch med_rai/heads/__init__.py
touch med_rai/data/__init__.py
touch med_rai/training/__init__.py
touch med_rai/tests/__init__.py
```

- [ ] **Step 4: Install dependencies**

```bash
pip install -r requirements.txt
```

Expected: All packages install without error. `import torch; torch.cuda.is_available()` returns `True`.

- [ ] **Step 5: Commit**

```bash
git add requirements.txt pytest.ini med_rai/
git commit -m "chore: project scaffold and dependencies"
```

---

## Task 2: SE(3) Utilities

**Files:**
- Create: `med_rai/utils/se3_utils.py`
- Create: `med_rai/tests/test_se3_utils.py`

- [ ] **Step 1: Write failing tests**

```python
# med_rai/tests/test_se3_utils.py
import torch
import pytest
from med_rai.utils.se3_utils import se3_log, se3_exp, geodesic_distance, clamp_to_workspace

def make_random_se3(batch=4):
    """Return random (R, t): R in SO(3), t in R^3."""
    from pytorch3d.transforms import random_rotations
    R = random_rotations(batch)          # (B, 3, 3)
    t = torch.randn(batch, 3) * 0.1     # (B, 3) small translations
    return R, t

def test_se3_log_shape():
    R, t = make_random_se3(4)
    xi = se3_log(R, t)
    assert xi.shape == (4, 6), f"Expected (4,6), got {xi.shape}"

def test_se3_exp_shape():
    xi = torch.randn(4, 6) * 0.1
    R, t = se3_exp(xi)
    assert R.shape == (4, 3, 3)
    assert t.shape == (4, 3)

def test_se3_roundtrip():
    """exp(log(T)) ≈ T for random SE(3) poses."""
    R, t = make_random_se3(100)
    xi = se3_log(R, t)
    R_hat, t_hat = se3_exp(xi)
    assert torch.allclose(R, R_hat, atol=1e-5), f"Max R error: {(R - R_hat).abs().max()}"
    assert torch.allclose(t, t_hat, atol=1e-5), f"Max t error: {(t - t_hat).abs().max()}"

def test_geodesic_distance_zero():
    R, t = make_random_se3(4)
    d = geodesic_distance(R, t, R, t)
    assert torch.allclose(d, torch.zeros(4), atol=1e-5)

def test_clamp_to_workspace():
    workspace = {"x": (-0.1, 0.1), "y": (-0.1, 0.1), "z": (0.05, 0.2)}
    t_out = torch.tensor([[0.5, 0.5, 0.5]])   # way outside workspace
    t_clamped = clamp_to_workspace(t_out, workspace)
    assert t_clamped[0, 0] <= 0.1
    assert t_clamped[0, 2] <= 0.2
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest med_rai/tests/test_se3_utils.py -v
```

Expected: `ImportError: cannot import name 'se3_log'`

- [ ] **Step 3: Implement se3_utils.py**

```python
# med_rai/utils/se3_utils.py
import torch
from pytorch3d.transforms import matrix_to_axis_angle, axis_angle_to_matrix

def se3_log(R: torch.Tensor, t: torch.Tensor) -> torch.Tensor:
    """
    Map SE(3) pose to se(3) Lie algebra 6-vector.
    R: (B, 3, 3) rotation matrix
    t: (B, 3) translation
    Returns xi: (B, 6) where xi[:, :3]=omega (axis-angle), xi[:, 3:]=t
    """
    omega = matrix_to_axis_angle(R)  # (B, 3)
    return torch.cat([omega, t], dim=-1)

def se3_exp(xi: torch.Tensor):
    """
    Map se(3) 6-vector to SE(3) pose.
    xi: (B, 6)
    Returns R: (B, 3, 3), t: (B, 3)
    """
    omega, t = xi[..., :3], xi[..., 3:]
    R = axis_angle_to_matrix(omega)
    return R, t

def geodesic_distance(R1: torch.Tensor, t1: torch.Tensor,
                      R2: torch.Tensor, t2: torch.Tensor) -> torch.Tensor:
    """
    Geodesic distance between two SE(3) poses.
    Returns scalar distance per batch element: (B,)
    """
    # Rotation distance: ||log(R1^T R2)||
    R_rel = R1.transpose(-1, -2) @ R2          # (B, 3, 3)
    omega_rel = matrix_to_axis_angle(R_rel)     # (B, 3)
    rot_dist = omega_rel.norm(dim=-1)           # (B,)
    # Translation distance
    t_dist = (t1 - t2).norm(dim=-1)             # (B,)
    return rot_dist + t_dist

def clamp_to_workspace(t: torch.Tensor, workspace: dict) -> torch.Tensor:
    """
    Clamp translation to surgical workspace bounds.
    workspace: {"x": (min, max), "y": (min, max), "z": (min, max)}
    t: (B, 3)
    """
    t = t.clone()
    for i, key in enumerate(["x", "y", "z"]):
        lo, hi = workspace[key]
        t[:, i] = t[:, i].clamp(lo, hi)
    return t
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest med_rai/tests/test_se3_utils.py -v
```

Expected: All 5 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add med_rai/utils/se3_utils.py med_rai/tests/test_se3_utils.py
git commit -m "feat: SE(3) log/exp maps, geodesic distance, workspace clamping"
```

---

## Task 3: Input Encoders

**Files:**
- Create: `med_rai/encoders/vision_encoder.py`
- Create: `med_rai/encoders/se3_encoder.py`
- Create: `med_rai/encoders/ft_encoder.py`
- Create: `med_rai/encoders/language_embedder.py`
- Create: `med_rai/tests/test_encoders.py`

- [ ] **Step 1: Write failing tests**

```python
# med_rai/tests/test_encoders.py
import torch
import pytest
from med_rai.encoders.vision_encoder import SurgicalViTEncoder
from med_rai.encoders.se3_encoder import SE3Encoder
from med_rai.encoders.ft_encoder import FTEncoder
from med_rai.encoders.language_embedder import LanguageEmbedder

D_OUT = 512  # all encoders project to this dim

def test_vision_encoder_output_shape():
    enc = SurgicalViTEncoder(d_out=D_OUT)
    imgs = torch.randn(2, 3, 224, 224)
    out = enc(imgs)
    assert out.shape == (2, D_OUT), f"Expected (2,{D_OUT}), got {out.shape}"

def test_vision_encoder_frozen():
    enc = SurgicalViTEncoder(d_out=D_OUT)
    # ViT backbone must be frozen; only linear probe trains
    vit_params = [p for n, p in enc.named_parameters() if "vit" in n and "probe" not in n]
    assert all(not p.requires_grad for p in vit_params), "ViT backbone must be frozen"

def test_se3_encoder_output_shape():
    enc = SE3Encoder(d_out=D_OUT)
    from pytorch3d.transforms import random_rotations
    R = random_rotations(2)
    t = torch.randn(2, 3)
    out = enc(R, t)
    assert out.shape == (2, D_OUT)

def test_ft_encoder_output_shape():
    enc = FTEncoder(d_out=D_OUT)
    ft = torch.randn(2, 6)  # 3 force + 3 torque
    out = enc(ft)
    assert out.shape == (2, D_OUT)

def test_language_embedder_output_shape():
    emb = LanguageEmbedder(d_out=D_OUT)
    tokens = torch.randint(0, 1000, (2, 16))  # (B, seq_len)
    out = emb(tokens)
    assert out.shape == (2, 16, D_OUT), f"Expected (2,16,{D_OUT}), got {out.shape}"
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest med_rai/tests/test_encoders.py -v
```

Expected: `ImportError: cannot import name 'SurgicalViTEncoder'`

- [ ] **Step 3: Implement vision_encoder.py**

```python
# med_rai/encoders/vision_encoder.py
import torch
import torch.nn as nn
import open_clip

class SurgicalViTEncoder(nn.Module):
    """CLIP ViT-L/14 with frozen backbone and trainable linear probe."""

    def __init__(self, d_out: int = 512):
        super().__init__()
        vit, _, self.preprocess = open_clip.create_model_and_transforms(
            "ViT-L-14", pretrained="openai"
        )
        self.vit = vit.visual
        for p in self.vit.parameters():
            p.requires_grad = False
        vit_dim = 1024  # ViT-L/14 output dim
        self.probe = nn.Linear(vit_dim, d_out)

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        """x: (B, 3, 224, 224) -> (B, d_out)"""
        with torch.no_grad():
            feats = self.vit(x)  # (B, vit_dim)
        return self.probe(feats)
```

- [ ] **Step 4: Implement se3_encoder.py**

```python
# med_rai/encoders/se3_encoder.py
import torch
import torch.nn as nn
from med_rai.utils.se3_utils import se3_log

class SE3Encoder(nn.Module):
    """Encodes SE(3) pose via log-map + MLP."""

    def __init__(self, d_out: int = 512):
        super().__init__()
        self.mlp = nn.Sequential(
            nn.Linear(6, 128),
            nn.SiLU(),
            nn.Linear(128, d_out),
        )

    def forward(self, R: torch.Tensor, t: torch.Tensor) -> torch.Tensor:
        """R: (B,3,3), t: (B,3) -> (B, d_out)"""
        xi = se3_log(R, t)   # (B, 6)
        return self.mlp(xi)
```

- [ ] **Step 5: Implement ft_encoder.py**

```python
# med_rai/encoders/ft_encoder.py
import torch
import torch.nn as nn

class FTEncoder(nn.Module):
    """MLP encoder for 6-DOF force/torque signals."""

    def __init__(self, d_out: int = 512):
        super().__init__()
        self.mlp = nn.Sequential(
            nn.Linear(6, 128),
            nn.SiLU(),
            nn.Linear(128, d_out),
        )

    def forward(self, ft: torch.Tensor) -> torch.Tensor:
        """ft: (B, 6) -> (B, d_out)"""
        return self.mlp(ft)
```

- [ ] **Step 6: Implement language_embedder.py**

```python
# med_rai/encoders/language_embedder.py
import torch
import torch.nn as nn

class LanguageEmbedder(nn.Module):
    """
    Projects Jamba token IDs into d_out-dimensional embeddings.
    Wraps Jamba's embed_tokens, frozen — only the projection trains.
    """

    def __init__(self, vocab_size: int = 65536, d_jamba: int = 4096, d_out: int = 512):
        super().__init__()
        # Lightweight embedding table (full Jamba embed_tokens loaded in backbone)
        self.embed = nn.Embedding(vocab_size, d_jamba)
        for p in self.embed.parameters():
            p.requires_grad = False
        self.proj = nn.Linear(d_jamba, d_out)

    def forward(self, tokens: torch.Tensor) -> torch.Tensor:
        """tokens: (B, seq_len) -> (B, seq_len, d_out)"""
        emb = self.embed(tokens)     # (B, seq_len, d_jamba)
        return self.proj(emb)        # (B, seq_len, d_out)
```

- [ ] **Step 7: Run tests to verify they pass**

```bash
pytest med_rai/tests/test_encoders.py -v
```

Expected: All 5 tests PASS.

- [ ] **Step 8: Commit**

```bash
git add med_rai/encoders/ med_rai/tests/test_encoders.py
git commit -m "feat: vision, SE(3), F/T, and language encoders"
```

---

## Task 4: Cross-Modal Alignment Layer

**Files:**
- Create: `med_rai/fusion/cross_modal_alignment.py`
- Create: `med_rai/tests/test_fusion.py`

- [ ] **Step 1: Write failing tests**

```python
# med_rai/tests/test_fusion.py
import torch
import pytest
from med_rai.fusion.cross_modal_alignment import CrossModalAlignment

B, SEQ, D = 2, 16, 512

def test_output_shape():
    cma = CrossModalAlignment(d_model=D, n_heads=8)
    # 4 modality token sequences: visual (1 token), kinematic (1), ft (1), language (SEQ)
    v = torch.randn(B, 1, D)
    k = torch.randn(B, 1, D)
    f = torch.randn(B, 1, D)
    l = torch.randn(B, SEQ, D)
    out = cma(v, k, f, l)
    assert out.shape == (B, SEQ + 3, D), f"Expected ({B},{SEQ+3},{D}), got {out.shape}"

def test_danger_signal_amplification():
    """High F/T norm + low-confidence visual should not collapse to zero."""
    cma = CrossModalAlignment(d_model=D, n_heads=8)
    v = torch.zeros(1, 1, D)       # low-confidence visual
    k = torch.randn(1, 1, D)
    f = torch.ones(1, 1, D) * 10.0  # high force spike
    l = torch.randn(1, 4, D)
    out = cma(v, k, f, l)
    assert out.norm() > 0, "Output collapsed to zero — cross-attention broken"
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest med_rai/tests/test_fusion.py -v
```

Expected: `ImportError: cannot import name 'CrossModalAlignment'`

- [ ] **Step 3: Implement cross_modal_alignment.py**

```python
# med_rai/fusion/cross_modal_alignment.py
import torch
import torch.nn as nn

class CrossModalAlignment(nn.Module):
    """
    Fuses 4 modality token sequences via cross-attention.
    Each modality attends to all others before the unified stream
    enters the Jamba backbone.

    Input token sequences (all projected to d_model):
        v_tokens:  (B, 1, d_model)   visual
        k_tokens:  (B, 1, d_model)   kinematics
        f_tokens:  (B, 1, d_model)   force/torque
        l_tokens:  (B, S, d_model)   language (variable length)

    Output:
        unified: (B, S+3, d_model)  concatenated, cross-attended token stream
    """

    def __init__(self, d_model: int = 512, n_heads: int = 8):
        super().__init__()
        self.cross_attn = nn.MultiheadAttention(d_model, n_heads, batch_first=True)
        self.norm = nn.LayerNorm(d_model)

    def forward(self, v: torch.Tensor, k: torch.Tensor,
                f: torch.Tensor, l: torch.Tensor) -> torch.Tensor:
        # Concatenate all modality tokens
        tokens = torch.cat([v, k, f, l], dim=1)   # (B, S+3, d_model)
        # Each token attends to all others (self + cross modal)
        attn_out, _ = self.cross_attn(tokens, tokens, tokens)
        return self.norm(tokens + attn_out)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest med_rai/tests/test_fusion.py -v
```

Expected: Both tests PASS.

- [ ] **Step 5: Commit**

```bash
git add med_rai/fusion/cross_modal_alignment.py med_rai/tests/test_fusion.py
git commit -m "feat: cross-modal alignment layer with multi-head self+cross attention"
```

---

## Task 5: Jamba QLoRA Backbone

**Files:**
- Create: `med_rai/backbone/jamba_backbone.py`
- Create: `med_rai/tests/test_backbone.py`

- [ ] **Step 1: Write failing tests**

```python
# med_rai/tests/test_backbone.py
import torch
import pytest
from med_rai.backbone.jamba_backbone import JambaBackbone

@pytest.fixture(scope="module")
def backbone():
    return JambaBackbone(model_id="ai21labs/Jamba-v0.1")

def test_only_lora_params_train(backbone):
    trainable = [n for n, p in backbone.named_parameters() if p.requires_grad]
    non_lora_trainable = [n for n in trainable if "lora_" not in n]
    assert len(non_lora_trainable) == 0, (
        f"Non-LoRA params are trainable: {non_lora_trainable[:5]}"
    )

def test_hidden_states_shape(backbone):
    # Simulate fused token stream: (B, seq_len, d_model_in)
    # Backbone accepts token IDs or pre-projected embeddings
    token_ids = torch.randint(0, 100, (1, 19))  # B=1, seq=3+16
    h = backbone.get_hidden_states(token_ids)
    assert h.ndim == 3, "Expected (B, seq, d_jamba)"
    assert h.shape[-1] == 4096, f"Expected d_jamba=4096, got {h.shape[-1]}"

def test_base_params_frozen(backbone):
    base_params = [p for n, p in backbone.named_parameters()
                   if "lora_" not in n]
    assert all(not p.requires_grad for p in base_params)
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest med_rai/tests/test_backbone.py -v
```

Expected: `ImportError: cannot import name 'JambaBackbone'`

- [ ] **Step 3: Implement jamba_backbone.py**

```python
# med_rai/backbone/jamba_backbone.py
import torch
import torch.nn as nn
from transformers import AutoModelForCausalLM, BitsAndBytesConfig
from peft import get_peft_model, LoraConfig, TaskType

class JambaBackbone(nn.Module):
    """
    Jamba hybrid Transformer+Mamba backbone loaded in 4-bit NF4 with QLoRA.
    LoRA applied to attention q/k/v/o projections (r=16, alpha=32).
    Returns hidden states from the final layer for downstream heads.
    """

    def __init__(self, model_id: str = "ai21labs/Jamba-v0.1",
                 lora_r: int = 16, lora_alpha: int = 32):
        super().__init__()
        bnb_config = BitsAndBytesConfig(
            load_in_4bit=True,
            bnb_4bit_quant_type="nf4",
            bnb_4bit_compute_dtype=torch.bfloat16,
            bnb_4bit_use_double_quant=True,
        )
        base = AutoModelForCausalLM.from_pretrained(
            model_id,
            quantization_config=bnb_config,
            device_map="auto",
            trust_remote_code=True,
        )
        lora_cfg = LoraConfig(
            r=lora_r,
            lora_alpha=lora_alpha,
            target_modules=["q_proj", "k_proj", "v_proj", "o_proj"],
            lora_dropout=0.05,
            bias="none",
            task_type=TaskType.CAUSAL_LM,
        )
        self.model = get_peft_model(base, lora_cfg)

    def get_hidden_states(self, input_ids: torch.Tensor) -> torch.Tensor:
        """
        input_ids: (B, seq_len) token IDs
        Returns: (B, seq_len, 4096) final hidden states
        """
        out = self.model(input_ids=input_ids, output_hidden_states=True)
        return out.hidden_states[-1]  # last layer hidden states

    def forward(self, input_ids: torch.Tensor) -> torch.Tensor:
        return self.get_hidden_states(input_ids)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest med_rai/tests/test_backbone.py -v
```

Expected: All 3 tests PASS. Note: first run downloads Jamba weights (~12GB); subsequent runs use cache.

- [ ] **Step 5: Commit**

```bash
git add med_rai/backbone/jamba_backbone.py med_rai/tests/test_backbone.py
git commit -m "feat: Jamba 4-bit NF4 QLoRA backbone with hidden state extraction"
```

---

## Task 6: RFM Policy Head

**Files:**
- Create: `med_rai/heads/rfm_policy_head.py`
- Create: `med_rai/tests/test_heads.py` (partial — extended in Task 7)

- [ ] **Step 1: Write failing tests**

```python
# med_rai/tests/test_heads.py
import torch
import pytest
from med_rai.heads.rfm_policy_head import RFMPolicyHead

B, SEQ, D_JAMBA, D_HIDDEN, H = 2, 19, 4096, 512, 10
SE3_DIM = 6

@pytest.fixture
def rfm_head():
    return RFMPolicyHead(d_jamba=D_JAMBA, d_hidden=D_HIDDEN, n_heads=8, n_layers=4)

def test_rfm_output_shape(rfm_head):
    xi_t = torch.randn(B, H, SE3_DIM)         # noisy trajectory in se(3)
    t = torch.rand(B)                           # timestep in [0,1]
    h = torch.randn(B, SEQ, D_JAMBA)           # Jamba hidden states
    v_theta = rfm_head(xi_t, t, h)
    assert v_theta.shape == (B, H, SE3_DIM), f"Expected ({B},{H},{SE3_DIM}), got {v_theta.shape}"

def test_rfm_no_nan(rfm_head):
    xi_t = torch.randn(B, H, SE3_DIM)
    t = torch.rand(B)
    h = torch.randn(B, SEQ, D_JAMBA)
    v_theta = rfm_head(xi_t, t, h)
    assert not torch.isnan(v_theta).any(), "NaN in RFM output"

def test_rfm_inference_ode(rfm_head):
    """4-step Euler ODE produces valid SE(3) pose (no NaN, finite)."""
    from med_rai.utils.se3_utils import se3_exp
    h = torch.randn(1, SEQ, D_JAMBA)
    xi = torch.randn(1, H, SE3_DIM)   # start from noise
    dt = 1.0 / 4
    for step in range(4):
        t = torch.full((1,), step * dt)
        v = rfm_head(xi, t, h)
        xi = xi + dt * v
    R, t_out = se3_exp(xi.reshape(-1, SE3_DIM))
    assert not torch.isnan(R).any()
    assert not torch.isnan(t_out).any()
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest med_rai/tests/test_heads.py -v
```

Expected: `ImportError: cannot import name 'RFMPolicyHead'`

- [ ] **Step 3: Implement rfm_policy_head.py**

```python
# med_rai/heads/rfm_policy_head.py
import torch
import torch.nn as nn
import math

class _RFMLayer(nn.Module):
    """Cross-attention + feedforward block for the RFM head."""

    def __init__(self, d_hidden: int, n_heads: int):
        super().__init__()
        self.cross_attn = nn.MultiheadAttention(d_hidden, n_heads, batch_first=True)
        self.ff = nn.Sequential(
            nn.Linear(d_hidden, d_hidden * 4),
            nn.GELU(),
            nn.Linear(d_hidden * 4, d_hidden),
        )
        self.norm1 = nn.LayerNorm(d_hidden)
        self.norm2 = nn.LayerNorm(d_hidden)

    def forward(self, x: torch.Tensor, kv: torch.Tensor) -> torch.Tensor:
        """x: (B, H, d_hidden), kv: (B, S, d_hidden)"""
        attn_out, _ = self.cross_attn(x, kv, kv)
        x = self.norm1(x + attn_out)
        x = self.norm2(x + self.ff(x))
        return x


class RFMPolicyHead(nn.Module):
    """
    Riemannian Flow Matching head on SE(3).

    Predicts velocity field v_theta(xi_t, t, h) in se(3) via:
    1. Project noisy trajectory xi_t (B, H, 6) -> (B, H, d_hidden)
    2. Add sinusoidal timestep embedding
    3. Cross-attend to Jamba hidden states h (B, S, 4096)
    4. Project back to (B, H, 6)

    Training loss: ||v_theta - (xi_1 - xi_0)||^2
    Inference: 4-step Euler ODE from xi_0 ~ N(0,I)
    """

    def __init__(self, d_jamba: int = 4096, d_hidden: int = 512,
                 n_heads: int = 8, n_layers: int = 4, traj_horizon: int = 10):
        super().__init__()
        self.traj_horizon = traj_horizon
        self.xi_proj = nn.Linear(6, d_hidden)
        self.t_embed = nn.Sequential(
            nn.Linear(d_hidden, d_hidden),
            nn.SiLU(),
            nn.Linear(d_hidden, d_hidden),
        )
        self.h_proj = nn.Linear(d_jamba, d_hidden)
        self.layers = nn.ModuleList([_RFMLayer(d_hidden, n_heads) for _ in range(n_layers)])
        self.out_proj = nn.Linear(d_hidden, 6)

    def _sinusoidal_embed(self, t: torch.Tensor, d: int) -> torch.Tensor:
        """t: (B,) -> (B, d) sinusoidal positional encoding."""
        half = d // 2
        freq = torch.exp(-math.log(10000) * torch.arange(half, device=t.device) / half)
        args = t[:, None] * freq[None, :]           # (B, half)
        emb = torch.cat([args.sin(), args.cos()], dim=-1)  # (B, d)
        return emb

    def forward(self, xi_t: torch.Tensor, t: torch.Tensor,
                h: torch.Tensor) -> torch.Tensor:
        """
        xi_t: (B, H, 6) noisy se(3) trajectory at time t
        t:    (B,)      timestep in [0, 1]
        h:    (B, S, d_jamba) Jamba hidden states
        Returns v_theta: (B, H, 6)
        """
        x = self.xi_proj(xi_t)                         # (B, H, d_hidden)
        t_emb = self.t_embed(self._sinusoidal_embed(t, x.shape[-1]))  # (B, d_hidden)
        x = x + t_emb.unsqueeze(1)                     # broadcast over H
        kv = self.h_proj(h)                             # (B, S, d_hidden)
        for layer in self.layers:
            x = layer(x, kv)
        return self.out_proj(x)                         # (B, H, 6)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest med_rai/tests/test_heads.py -v
```

Expected: All 3 RFM tests PASS.

- [ ] **Step 5: Commit**

```bash
git add med_rai/heads/rfm_policy_head.py med_rai/tests/test_heads.py
git commit -m "feat: Riemannian Flow Matching policy head on SE(3) with cross-attention conditioning"
```

---

## Task 7: Text Head and Gesture Classifier

**Files:**
- Create: `med_rai/heads/text_head.py`
- Create: `med_rai/heads/gesture_head.py`
- Modify: `med_rai/tests/test_heads.py` (append new tests)

- [ ] **Step 1: Append failing tests to test_heads.py**

```python
# Append to med_rai/tests/test_heads.py
from med_rai.heads.text_head import TextHead
from med_rai.heads.gesture_head import GestureHead

N_GESTURES = 15

def test_text_head_output_shape():
    head = TextHead(d_jamba=D_JAMBA, vocab_size=65536)
    h = torch.randn(B, SEQ, D_JAMBA)
    logits = head(h)
    assert logits.shape == (B, SEQ, 65536), f"Got {logits.shape}"

def test_text_head_warning_token():
    """TextHead must have a WARNING token at a known index."""
    head = TextHead(d_jamba=D_JAMBA, vocab_size=65536)
    assert hasattr(head, "WARNING_TOKEN_ID"), "TextHead must expose WARNING_TOKEN_ID"
    assert isinstance(head.WARNING_TOKEN_ID, int)

def test_gesture_head_output_shape():
    head = GestureHead(d_jamba=D_JAMBA, n_gestures=N_GESTURES)
    # Use pooled hidden state (mean over sequence)
    h = torch.randn(B, SEQ, D_JAMBA)
    logits = head(h)
    assert logits.shape == (B, N_GESTURES), f"Got {logits.shape}"
```

- [ ] **Step 2: Run new tests to verify they fail**

```bash
pytest med_rai/tests/test_heads.py::test_text_head_output_shape -v
```

Expected: `ImportError: cannot import name 'TextHead'`

- [ ] **Step 3: Implement text_head.py**

```python
# med_rai/heads/text_head.py
import torch
import torch.nn as nn

# Reserve token index 1 for WARNING — must match tokenizer in Phase 3 training
_WARNING_TOKEN_ID = 1

class TextHead(nn.Module):
    """
    LM head projecting Jamba hidden states to vocabulary logits.
    Exposes WARNING_TOKEN_ID for the danger override logic.
    """
    WARNING_TOKEN_ID: int = _WARNING_TOKEN_ID

    def __init__(self, d_jamba: int = 4096, vocab_size: int = 65536):
        super().__init__()
        self.proj = nn.Linear(d_jamba, vocab_size, bias=False)

    def forward(self, h: torch.Tensor) -> torch.Tensor:
        """h: (B, S, d_jamba) -> logits: (B, S, vocab_size)"""
        return self.proj(h)
```

- [ ] **Step 4: Implement gesture_head.py**

```python
# med_rai/heads/gesture_head.py
import torch
import torch.nn as nn

# JIGSAWS gesture vocabulary (G1–G15)
GESTURE_NAMES = [
    "reaching_for_needle_with_right_hand",
    "positioning_needle",
    "pushing_needle_through_tissue",
    "transferring_needle_from_left_to_right",
    "moving_to_center",
    "pulling_suture_with_left_hand",
    "pulling_suture_with_right_hand",
    "orienting_needle",
    "using_right_hand_to_help_tighten_suture",
    "loosening_more_suture",
    "dropping_suture_at_end_and_moving_to_center",
    "reaching_for_needle_with_left_hand",
    "making_figure_of_eight",
    "pulling_needle_out_of_tissue",  # G14
    "idle",                          # G15
]

class GestureHead(nn.Module):
    """Linear classifier over JIGSAWS 15-gesture vocabulary."""

    def __init__(self, d_jamba: int = 4096, n_gestures: int = 15):
        super().__init__()
        self.pool = lambda h: h.mean(dim=1)   # mean-pool over sequence
        self.classifier = nn.Linear(d_jamba, n_gestures)

    def forward(self, h: torch.Tensor) -> torch.Tensor:
        """h: (B, S, d_jamba) -> logits: (B, n_gestures)"""
        pooled = self.pool(h)            # (B, d_jamba)
        return self.classifier(pooled)   # (B, n_gestures)
```

- [ ] **Step 5: Run all head tests**

```bash
pytest med_rai/tests/test_heads.py -v
```

Expected: All 6 tests PASS.

- [ ] **Step 6: Commit**

```bash
git add med_rai/heads/text_head.py med_rai/heads/gesture_head.py med_rai/tests/test_heads.py
git commit -m "feat: text LM head with WARNING token and gesture classifier"
```

---

## Task 8: Loss Function

**Files:**
- Create: `med_rai/loss.py`
- Create: `med_rai/tests/test_loss.py`

- [ ] **Step 1: Write failing tests**

```python
# med_rai/tests/test_loss.py
import torch
import pytest
from med_rai.loss import MedRAILoss

B, H, SE3_DIM, SEQ, VOCAB, N_GESTURES = 2, 10, 6, 19, 65536, 15

@pytest.fixture
def loss_fn():
    return MedRAILoss(lambda_rfm=1.0, lambda_text=0.5, lambda_gesture=0.3)

def test_loss_shape(loss_fn):
    v_pred = torch.randn(B, H, SE3_DIM)
    u_target = torch.randn(B, H, SE3_DIM)
    text_logits = torch.randn(B, SEQ, VOCAB)
    text_targets = torch.randint(0, VOCAB, (B, SEQ))
    gesture_logits = torch.randn(B, N_GESTURES)
    gesture_targets = torch.randint(0, N_GESTURES, (B,))
    total, components = loss_fn(v_pred, u_target, text_logits,
                                text_targets, gesture_logits, gesture_targets)
    assert total.shape == (), "Total loss must be scalar"
    assert set(components.keys()) == {"rfm", "text", "gesture"}

def test_loss_weights(loss_fn):
    """Verify lambdas are applied correctly."""
    v_pred = torch.zeros(1, H, SE3_DIM)
    u_target = torch.ones(1, H, SE3_DIM)
    text_logits = torch.zeros(1, SEQ, VOCAB)
    text_targets = torch.zeros(1, SEQ, dtype=torch.long)
    gesture_logits = torch.zeros(1, N_GESTURES)
    gesture_targets = torch.zeros(1, dtype=torch.long)
    total, c = loss_fn(v_pred, u_target, text_logits,
                       text_targets, gesture_logits, gesture_targets)
    expected = 1.0 * c["rfm"] + 0.5 * c["text"] + 0.3 * c["gesture"]
    assert torch.isclose(total, expected, atol=1e-5)

def test_no_nan_loss(loss_fn):
    v_pred = torch.randn(B, H, SE3_DIM)
    u_target = torch.randn(B, H, SE3_DIM)
    text_logits = torch.randn(B, SEQ, VOCAB)
    text_targets = torch.randint(0, VOCAB, (B, SEQ))
    gesture_logits = torch.randn(B, N_GESTURES)
    gesture_targets = torch.randint(0, N_GESTURES, (B,))
    total, _ = loss_fn(v_pred, u_target, text_logits,
                       text_targets, gesture_logits, gesture_targets)
    assert not torch.isnan(total)
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest med_rai/tests/test_loss.py -v
```

Expected: `ImportError: cannot import name 'MedRAILoss'`

- [ ] **Step 3: Implement loss.py**

```python
# med_rai/loss.py
import torch
import torch.nn as nn
from typing import Tuple, Dict

class MedRAILoss(nn.Module):
    """
    Weighted multi-task loss:
        L_total = lambda_rfm * L_rfm + lambda_text * L_text + lambda_gesture * L_gesture

    L_rfm:     Flow Matching MSE in se(3): ||v_theta - (xi_1 - xi_0)||^2
    L_text:    Cross-entropy over vocabulary (risk alerts / surgical logs)
    L_gesture: Cross-entropy over 15 JIGSAWS gestures
    """

    def __init__(self, lambda_rfm: float = 1.0,
                 lambda_text: float = 0.5,
                 lambda_gesture: float = 0.3):
        super().__init__()
        self.lambda_rfm = lambda_rfm
        self.lambda_text = lambda_text
        self.lambda_gesture = lambda_gesture
        self.ce = nn.CrossEntropyLoss()

    def forward(
        self,
        v_pred: torch.Tensor,       # (B, H, 6) predicted velocity
        u_target: torch.Tensor,     # (B, H, 6) target velocity = xi_1 - xi_0
        text_logits: torch.Tensor,  # (B, S, vocab)
        text_targets: torch.Tensor, # (B, S)
        gesture_logits: torch.Tensor,  # (B, n_gestures)
        gesture_targets: torch.Tensor, # (B,)
    ) -> Tuple[torch.Tensor, Dict[str, torch.Tensor]]:

        l_rfm = ((v_pred - u_target) ** 2).mean()
        l_text = self.ce(text_logits.reshape(-1, text_logits.size(-1)),
                         text_targets.reshape(-1))
        l_gesture = self.ce(gesture_logits, gesture_targets)

        total = self.lambda_rfm * l_rfm + self.lambda_text * l_text + self.lambda_gesture * l_gesture
        return total, {"rfm": l_rfm, "text": l_text, "gesture": l_gesture}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest med_rai/tests/test_loss.py -v
```

Expected: All 3 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add med_rai/loss.py med_rai/tests/test_loss.py
git commit -m "feat: weighted multi-task loss (RFM + text CE + gesture CE)"
```

---

## Task 9: MedRAI LightningModule

**Files:**
- Create: `med_rai/model.py`
- Create: `med_rai/tests/test_model.py`

- [ ] **Step 1: Write failing tests**

```python
# med_rai/tests/test_model.py
import torch
import pytest
from unittest.mock import MagicMock, patch
from med_rai.model import MedRAI

B, SEQ_L, H = 1, 8, 10

def make_batch():
    from pytorch3d.transforms import random_rotations
    return {
        "images":   torch.randn(B, 3, 224, 224),
        "R":        random_rotations(B),
        "t":        torch.randn(B, 3) * 0.05,
        "ft":       torch.randn(B, 6),
        "token_ids": torch.randint(0, 1000, (B, SEQ_L)),
        "xi_traj":  torch.randn(B, H, 6),   # ground-truth se(3) trajectory
        "gesture_labels": torch.randint(0, 15, (B,)),
        "text_targets":   torch.randint(0, 65536, (B, SEQ_L + 3)),
    }

@pytest.fixture(scope="module")
def model():
    # Use a tiny mock backbone to avoid downloading Jamba in unit tests
    with patch("med_rai.model.JambaBackbone") as MockBackbone:
        mock_bb = MagicMock()
        mock_bb.return_value = torch.randn(B, SEQ_L + 3, 4096)
        MockBackbone.return_value = mock_bb
        m = MedRAI()
    return m

def test_forward_output_keys(model):
    batch = make_batch()
    out = model(batch)
    assert "v_theta" in out
    assert "text_logits" in out
    assert "gesture_logits" in out

def test_forward_shapes(model):
    batch = make_batch()
    out = model(batch)
    assert out["v_theta"].shape == (B, H, 6)
    assert out["gesture_logits"].shape == (B, 15)

def test_danger_override(model):
    """If Text Head emits WARNING token, v_theta must be zeroed."""
    batch = make_batch()
    with patch.object(model, "_check_danger", return_value=True):
        out = model(batch)
    assert out["v_theta"].abs().sum() == 0, "Trajectory not zeroed on WARNING"
    assert out["forced_gesture"] == "RETRACT"
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest med_rai/tests/test_model.py -v
```

Expected: `ImportError: cannot import name 'MedRAI'`

- [ ] **Step 3: Implement model.py**

```python
# med_rai/model.py
import torch
import torch.nn as nn
import pytorch_lightning as pl
from typing import Dict, Any

from med_rai.encoders.vision_encoder import SurgicalViTEncoder
from med_rai.encoders.se3_encoder import SE3Encoder
from med_rai.encoders.ft_encoder import FTEncoder
from med_rai.encoders.language_embedder import LanguageEmbedder
from med_rai.fusion.cross_modal_alignment import CrossModalAlignment
from med_rai.backbone.jamba_backbone import JambaBackbone
from med_rai.heads.rfm_policy_head import RFMPolicyHead
from med_rai.heads.text_head import TextHead
from med_rai.heads.gesture_head import GestureHead
from med_rai.loss import MedRAILoss
from med_rai.utils.se3_utils import clamp_to_workspace

WORKSPACE = {"x": (-0.15, 0.15), "y": (-0.15, 0.15), "z": (0.03, 0.25)}
D_HIDDEN = 512
N_STEPS_ODE = 4

class MedRAI(pl.LightningModule):
    def __init__(self, lr: float = 1e-4,
                 lambda_rfm: float = 1.0,
                 lambda_text: float = 0.5,
                 lambda_gesture: float = 0.3):
        super().__init__()
        self.save_hyperparameters()

        # Encoders
        self.vision_enc = SurgicalViTEncoder(d_out=D_HIDDEN)
        self.se3_enc    = SE3Encoder(d_out=D_HIDDEN)
        self.ft_enc     = FTEncoder(d_out=D_HIDDEN)
        self.lang_emb   = LanguageEmbedder(d_out=D_HIDDEN)

        # Fusion
        self.cross_modal = CrossModalAlignment(d_model=D_HIDDEN, n_heads=8)

        # Backbone (projects fused tokens to Jamba token IDs via argmax for now)
        self.backbone = JambaBackbone()

        # Heads
        self.rfm_head     = RFMPolicyHead(d_jamba=4096, d_hidden=D_HIDDEN)
        self.text_head    = TextHead(d_jamba=4096)
        self.gesture_head = GestureHead(d_jamba=4096)

        # Loss
        self.loss_fn = MedRAILoss(lambda_rfm, lambda_text, lambda_gesture)

    def forward(self, batch: Dict[str, torch.Tensor]) -> Dict[str, Any]:
        # Encode modalities
        v_tok = self.vision_enc(batch["images"]).unsqueeze(1)    # (B,1,D)
        k_tok = self.se3_enc(batch["R"], batch["t"]).unsqueeze(1)
        f_tok = self.ft_enc(batch["ft"]).unsqueeze(1)
        l_tok = self.lang_emb(batch["token_ids"])                # (B,S,D)

        # Cross-modal fusion
        fused = self.cross_modal(v_tok, k_tok, f_tok, l_tok)    # (B,S+3,D)

        # Backbone: project fused tokens to token IDs for Jamba
        # (Simplified: use argmax over vocab — full training uses soft embeddings)
        token_ids = fused.argmax(dim=-1).clamp(0, 999)          # (B, S+3)
        h = self.backbone(token_ids)                             # (B, S+3, 4096)

        # Output heads
        B, _, _ = h.shape
        xi_0 = torch.randn(B, self.rfm_head.traj_horizon, 6, device=h.device)
        v_theta = self._ode_solve(xi_0, h)

        text_logits    = self.text_head(h)
        gesture_logits = self.gesture_head(h)

        # Danger override
        danger = self._check_danger(text_logits)
        if danger:
            v_theta = torch.zeros_like(v_theta)
            return {"v_theta": v_theta, "text_logits": text_logits,
                    "gesture_logits": gesture_logits, "forced_gesture": "RETRACT"}

        return {"v_theta": v_theta, "text_logits": text_logits,
                "gesture_logits": gesture_logits}

    def _ode_solve(self, xi: torch.Tensor, h: torch.Tensor) -> torch.Tensor:
        """4-step Euler ODE integration in se(3)."""
        dt = 1.0 / N_STEPS_ODE
        for step in range(N_STEPS_ODE):
            t = torch.full((xi.shape[0],), step * dt, device=xi.device)
            v = self.rfm_head(xi, t, h)
            xi = xi + dt * v
        # Clamp translation component to workspace
        from med_rai.utils.se3_utils import se3_exp
        _, t_out = se3_exp(xi.reshape(-1, 6))
        t_out = clamp_to_workspace(t_out, WORKSPACE)
        return xi

    def _check_danger(self, text_logits: torch.Tensor) -> bool:
        """Returns True if WARNING token is the top prediction for any token position."""
        preds = text_logits.argmax(dim=-1)   # (B, S)
        return (preds == TextHead.WARNING_TOKEN_ID).any().item()

    def training_step(self, batch, batch_idx):
        out = self(batch)
        total, components = self.loss_fn(
            out["v_theta"], batch["xi_traj"],
            out["text_logits"], batch["text_targets"],
            out["gesture_logits"], batch["gesture_labels"],
        )
        self.log("train/loss", total, prog_bar=True)
        for k, v in components.items():
            self.log(f"train/loss_{k}", v)
        return total

    def validation_step(self, batch, batch_idx):
        out = self(batch)
        total, components = self.loss_fn(
            out["v_theta"], batch["xi_traj"],
            out["text_logits"], batch["text_targets"],
            out["gesture_logits"], batch["gesture_labels"],
        )
        self.log("val/loss", total, prog_bar=True)
        for k, v in components.items():
            self.log(f"val/loss_{k}", v)

    def configure_optimizers(self):
        import bitsandbytes as bnb
        return bnb.optim.AdamW8bit(self.parameters(), lr=self.hparams.lr)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest med_rai/tests/test_model.py -v
```

Expected: All 3 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add med_rai/model.py med_rai/tests/test_model.py
git commit -m "feat: MedRAI LightningModule with danger override and 4-step ODE inference"
```

---

## Task 10: JIGSAWS Dataset

**Files:**
- Create: `med_rai/data/jigsaws_dataset.py`
- Create: `med_rai/data/collate.py`

> **Note:** JIGSAWS requires manual access request at https://cirl.lcsr.jhu.edu/research/hmm/datasets/jigsaws_release/
> Download and place under `data/jigsaws/` with structure:
> ```
> data/jigsaws/
>   Knot_Tying/
>     kinematics/AllGestures/   # .txt kinematic files
>     video/                    # .avi video files
>     transcriptions/           # gesture label files
> ```

- [ ] **Step 1: Write failing tests**

```python
# med_rai/tests/test_dataset.py  (new file)
import torch
import pytest
import os
from med_rai.data.jigsaws_dataset import JIGSAWSDataset
from med_rai.data.collate import medrai_collate

JIGSAWS_ROOT = os.environ.get("JIGSAWS_ROOT", "data/jigsaws")

@pytest.mark.skipif(not os.path.exists(JIGSAWS_ROOT),
                    reason="JIGSAWS data not available")
def test_dataset_len():
    ds = JIGSAWSDataset(root=JIGSAWS_ROOT, task="Knot_Tying", split="train")
    assert len(ds) > 0

@pytest.mark.skipif(not os.path.exists(JIGSAWS_ROOT),
                    reason="JIGSAWS data not available")
def test_item_keys():
    ds = JIGSAWSDataset(root=JIGSAWS_ROOT, task="Knot_Tying", split="train")
    item = ds[0]
    assert "images" in item        # (3, 224, 224)
    assert "R" in item             # (3, 3)
    assert "t" in item             # (3,)
    assert "ft" in item            # (6,) synthetic
    assert "token_ids" in item     # (S,)
    assert "xi_traj" in item       # (H, 6)
    assert "gesture_labels" in item
    assert "text_targets" in item

def test_synthetic_collate():
    """Collate should pad variable-length token sequences."""
    from pytorch3d.transforms import random_rotations
    samples = [
        {"images": torch.randn(3, 224, 224), "R": random_rotations(1).squeeze(0),
         "t": torch.randn(3), "ft": torch.randn(6), "token_ids": torch.randint(0,100,(8,)),
         "xi_traj": torch.randn(10, 6), "gesture_labels": torch.tensor(0),
         "text_targets": torch.randint(0, 100, (11,))},
        {"images": torch.randn(3, 224, 224), "R": random_rotations(1).squeeze(0),
         "t": torch.randn(3), "ft": torch.randn(6), "token_ids": torch.randint(0,100,(12,)),
         "xi_traj": torch.randn(10, 6), "gesture_labels": torch.tensor(3),
         "text_targets": torch.randint(0, 100, (15,))},
    ]
    batch = medrai_collate(samples)
    assert batch["token_ids"].shape[0] == 2
    assert batch["token_ids"].shape[1] == 12   # padded to max
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest med_rai/tests/test_dataset.py::test_synthetic_collate -v
```

Expected: `ImportError: cannot import name 'JIGSAWSDataset'`

- [ ] **Step 3: Implement jigsaws_dataset.py**

```python
# med_rai/data/jigsaws_dataset.py
import os
import numpy as np
import torch
from torch.utils.data import Dataset
from torchvision import transforms
from pytorch3d.transforms import axis_angle_to_matrix
from med_rai.utils.se3_utils import se3_log

TRAJ_HORIZON = 10
IMG_SIZE = 224
FT_NOISE_STD = 0.01   # synthetic F/T noise level

_TRANSFORM = transforms.Compose([
    transforms.Resize((IMG_SIZE, IMG_SIZE)),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225]),
])

# Leave-one-user-out splits for JIGSAWS (8 subjects)
_SPLITS = {
    "train": ["B", "C", "D", "E", "F", "G", "H"],
    "val":   ["A"],
}

class JIGSAWSDataset(Dataset):
    """
    Loads JIGSAWS kinematic + video + gesture data.
    Force/torque is simulated as Gaussian noise (JIGSAWS has no F/T).
    Returns one training sample = one frame with a TRAJ_HORIZON-step
    kinematic look-ahead as the trajectory target.
    """

    def __init__(self, root: str, task: str = "Knot_Tying",
                 split: str = "train", seq_len: int = 8):
        self.root = root
        self.task = task
        self.seq_len = seq_len
        subjects = _SPLITS[split]
        self.samples = self._load_samples(subjects)

    def _load_samples(self, subjects):
        samples = []
        kin_dir = os.path.join(self.root, self.task, "kinematics", "AllGestures")
        trn_dir = os.path.join(self.root, self.task, "transcriptions")
        for fname in os.listdir(kin_dir):
            subject = fname[fname.index("_") + 1]   # e.g. "Knot_Tying_B001.txt" -> "B"
            if subject not in subjects:
                continue
            kin_path = os.path.join(kin_dir, fname)
            trn_path = os.path.join(trn_dir, fname)
            kin = np.loadtxt(kin_path)               # (T, 76)
            gestures = self._load_gestures(trn_path, len(kin))
            for i in range(len(kin) - TRAJ_HORIZON):
                samples.append((kin, gestures, i))
        return samples

    def _load_gestures(self, path, n_frames):
        labels = np.zeros(n_frames, dtype=int)
        with open(path) as f:
            for line in f:
                parts = line.strip().split()
                if len(parts) == 3:
                    start, end, g = int(parts[0]), int(parts[1]), int(parts[2][1:]) - 1
                    labels[start:end] = g
        return labels

    def __len__(self):
        return len(self.samples)

    def __getitem__(self, idx):
        kin, gestures, i = self.samples[idx]

        # End-effector pose from PSM1 columns (0:3 position, 3:12 rotation matrix)
        pos = torch.tensor(kin[i, 0:3], dtype=torch.float32)
        rot_flat = torch.tensor(kin[i, 3:12], dtype=torch.float32).reshape(3, 3)

        # Trajectory: next TRAJ_HORIZON frames in se(3)
        traj_xi = []
        for j in range(i, i + TRAJ_HORIZON):
            p_j = torch.tensor(kin[j, 0:3], dtype=torch.float32)
            R_j = torch.tensor(kin[j, 3:12], dtype=torch.float32).reshape(3, 3)
            traj_xi.append(se3_log(R_j.unsqueeze(0), p_j.unsqueeze(0)).squeeze(0))
        xi_traj = torch.stack(traj_xi, dim=0)   # (H, 6)

        # Synthetic F/T (Gaussian noise)
        ft = torch.randn(6) * FT_NOISE_STD

        # Dummy image (replace with actual video frame loader if video available)
        image = torch.randn(3, IMG_SIZE, IMG_SIZE)

        # Dummy token IDs (replace with actual surgeon speech tokens)
        token_ids = torch.zeros(self.seq_len, dtype=torch.long)

        return {
            "images":        image,
            "R":             rot_flat,
            "t":             pos,
            "ft":            ft,
            "token_ids":     token_ids,
            "xi_traj":       xi_traj,
            "gesture_labels": torch.tensor(int(gestures[i]), dtype=torch.long),
            "text_targets":   torch.zeros(self.seq_len + 3, dtype=torch.long),
        }
```

- [ ] **Step 4: Implement collate.py**

```python
# med_rai/data/collate.py
import torch
from torch.nn.utils.rnn import pad_sequence
from typing import List, Dict

def medrai_collate(samples: List[Dict]) -> Dict:
    """Collates MedRAI samples, padding variable-length token sequences."""
    batch = {}
    for key in samples[0]:
        vals = [s[key] for s in samples]
        if key in ("token_ids", "text_targets"):
            batch[key] = pad_sequence(vals, batch_first=True, padding_value=0)
        else:
            batch[key] = torch.stack(vals)
    return batch
```

- [ ] **Step 5: Run tests**

```bash
pytest med_rai/tests/test_dataset.py::test_synthetic_collate -v
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add med_rai/data/jigsaws_dataset.py med_rai/data/collate.py med_rai/tests/test_dataset.py
git commit -m "feat: JIGSAWS dataset loader with synthetic F/T and collate function"
```

---

## Task 11: Synthetic SE(3) Dataset

**Files:**
- Create: `med_rai/data/synthetic_se3.py`

- [ ] **Step 1: Write failing tests (append to test_dataset.py)**

```python
# Append to med_rai/tests/test_dataset.py
from med_rai.data.synthetic_se3 import SyntheticSE3Dataset

def test_synthetic_dataset_len():
    ds = SyntheticSE3Dataset(n_samples=100)
    assert len(ds) == 100

def test_synthetic_item_shape():
    ds = SyntheticSE3Dataset(n_samples=10)
    item = ds[0]
    assert item["xi_0"].shape == (6,)    # start pose
    assert item["xi_1"].shape == (6,)    # end pose
    assert item["xi_traj"].shape == (10, 6)  # geodesic interpolation

def test_synthetic_geodesic_linearity():
    """Interpolated trajectory must be a straight line in se(3)."""
    ds = SyntheticSE3Dataset(n_samples=10)
    item = ds[0]
    xi_0, xi_1 = item["xi_0"], item["xi_1"]
    xi_traj = item["xi_traj"]
    for step in range(10):
        t = (step + 1) / 10
        expected = (1 - t) * xi_0 + t * xi_1
        assert torch.allclose(xi_traj[step], expected, atol=1e-5)
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest med_rai/tests/test_dataset.py::test_synthetic_dataset_len -v
```

Expected: `ImportError: cannot import name 'SyntheticSE3Dataset'`

- [ ] **Step 3: Implement synthetic_se3.py**

```python
# med_rai/data/synthetic_se3.py
import torch
from torch.utils.data import Dataset
from pytorch3d.transforms import random_rotations
from med_rai.utils.se3_utils import se3_log

TRAJ_HORIZON = 10

class SyntheticSE3Dataset(Dataset):
    """
    Generates synthetic geodesic trajectories on SE(3) for RFM head warmup.
    Each sample is a straight-line path (in se(3)) between two random SE(3) poses.
    This matches the Flow Matching training objective: straight-line interpolation.
    """

    def __init__(self, n_samples: int = 10000, t_scale: float = 0.05):
        self.n_samples = n_samples
        self.t_scale = t_scale

    def __len__(self):
        return self.n_samples

    def __getitem__(self, idx):
        # Random start and end SE(3) poses
        R0 = random_rotations(1).squeeze(0)
        t0 = torch.randn(3) * self.t_scale
        R1 = random_rotations(1).squeeze(0)
        t1 = torch.randn(3) * self.t_scale

        xi_0 = se3_log(R0.unsqueeze(0), t0.unsqueeze(0)).squeeze(0)  # (6,)
        xi_1 = se3_log(R1.unsqueeze(0), t1.unsqueeze(0)).squeeze(0)  # (6,)

        # Straight-line geodesic: xi(t) = (1-t)*xi_0 + t*xi_1
        traj = []
        for step in range(TRAJ_HORIZON):
            t = (step + 1) / TRAJ_HORIZON
            traj.append((1 - t) * xi_0 + t * xi_1)
        xi_traj = torch.stack(traj, dim=0)   # (H, 6)

        return {"xi_0": xi_0, "xi_1": xi_1, "xi_traj": xi_traj}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest med_rai/tests/test_dataset.py -v -k "synthetic"
```

Expected: All 3 synthetic tests PASS.

- [ ] **Step 5: Commit**

```bash
git add med_rai/data/synthetic_se3.py med_rai/tests/test_dataset.py
git commit -m "feat: synthetic SE(3) geodesic dataset for RFM head warmup"
```

---

## Task 12: Phase 1 Trainer — Encoder Warmup

**Files:**
- Create: `med_rai/training/phase1_train.py`

- [ ] **Step 1: Implement phase1_train.py**

```python
# med_rai/training/phase1_train.py
"""
Phase 1: Encoder & Cross-Modal Alignment Warmup.
Trains: SurgicalViTEncoder linear probe, SE3Encoder, FTEncoder, CrossModalAlignment.
Freezes: JambaBackbone, all output heads.
Objective: MSE reconstruction on se(3) kinematic sequences +
           InfoNCE contrastive loss between visual and kinematic tokens.
"""
import torch
import torch.nn.functional as F
import pytorch_lightning as pl
from pytorch_lightning.callbacks import ModelCheckpoint
from pytorch_lightning.loggers import WandbLogger
from torch.utils.data import DataLoader, random_split
import os

from med_rai.model import MedRAI
from med_rai.data.jigsaws_dataset import JIGSAWSDataset
from med_rai.data.collate import medrai_collate

JIGSAWS_ROOT = os.environ.get("JIGSAWS_ROOT", "data/jigsaws")
CKPT_DIR = os.environ.get("CKPT_DIR", "/content/drive/MyDrive/med_rai/checkpoints")


class Phase1Module(pl.LightningModule):
    """Wraps MedRAI for Phase 1 — only encoder params are trainable."""

    def __init__(self, lr: float = 1e-4):
        super().__init__()
        self.save_hyperparameters()
        self.model = MedRAI()
        self._freeze_non_encoders()

    def _freeze_non_encoders(self):
        """Freeze backbone and all heads; leave encoders + cross_modal trainable."""
        for name, param in self.model.named_parameters():
            if any(name.startswith(p) for p in
                   ["backbone", "rfm_head", "text_head", "gesture_head"]):
                param.requires_grad = False

    def _infonce_loss(self, v: torch.Tensor, k: torch.Tensor, temp: float = 0.07):
        """InfoNCE contrastive loss between visual and kinematic tokens."""
        v = F.normalize(v, dim=-1)
        k = F.normalize(k, dim=-1)
        logits = (v @ k.T) / temp
        labels = torch.arange(len(v), device=v.device)
        return (F.cross_entropy(logits, labels) + F.cross_entropy(logits.T, labels)) / 2

    def training_step(self, batch, batch_idx):
        v_tok = self.model.vision_enc(batch["images"])     # (B, D)
        k_tok = self.model.se3_enc(batch["R"], batch["t"]) # (B, D)
        contrastive = self._infonce_loss(v_tok, k_tok)
        # Reconstruction: encode then reconstruct se(3) via ft+kinematic path
        xi_pred = self.model.se3_enc(batch["R"], batch["t"])
        recon = F.mse_loss(xi_pred, batch["xi_traj"][:, 0, :])
        loss = contrastive + recon
        self.log("phase1/contrastive", contrastive)
        self.log("phase1/recon", recon)
        self.log("phase1/loss", loss, prog_bar=True)
        return loss

    def configure_optimizers(self):
        import bitsandbytes as bnb
        params = [p for p in self.parameters() if p.requires_grad]
        return bnb.optim.AdamW8bit(params, lr=self.hparams.lr)


def run_phase1():
    ds = JIGSAWSDataset(root=JIGSAWS_ROOT, task="Knot_Tying", split="train")
    val_ds = JIGSAWSDataset(root=JIGSAWS_ROOT, task="Knot_Tying", split="val")
    train_loader = DataLoader(ds, batch_size=16, shuffle=True,
                              num_workers=2, collate_fn=medrai_collate)
    val_loader   = DataLoader(val_ds, batch_size=16,
                              num_workers=2, collate_fn=medrai_collate)
    module = Phase1Module(lr=1e-4)
    ckpt_cb = ModelCheckpoint(dirpath=CKPT_DIR, filename="phase1-{epoch}-{phase1/loss:.3f}",
                              save_top_k=3, monitor="phase1/loss", every_n_train_steps=500)
    trainer = pl.Trainer(
        max_epochs=5,
        accelerator="gpu", devices=1,
        precision="bf16-mixed",
        gradient_clip_val=1.0,
        callbacks=[ckpt_cb],
        logger=WandbLogger(project="med-rai", name="phase1") if os.environ.get("WANDB_API_KEY") else True,
    )
    trainer.fit(module, train_loader, val_loader)
    return ckpt_cb.best_model_path


if __name__ == "__main__":
    run_phase1()
```

- [ ] **Step 2: Verify syntax**

```bash
python -c "from med_rai.training.phase1_train import Phase1Module; print('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add med_rai/training/phase1_train.py
git commit -m "feat: Phase 1 encoder warmup trainer with InfoNCE + reconstruction loss"
```

---

## Task 13: Phase 2 Trainer — RFM Head Warmup

**Files:**
- Create: `med_rai/training/phase2_train.py`

- [ ] **Step 1: Implement phase2_train.py**

```python
# med_rai/training/phase2_train.py
"""
Phase 2: RFM Policy Head Warmup on synthetic SE(3) geodesic trajectories.
Trains: RFMPolicyHead only.
Freezes: Everything else.
Objective: Flow Matching MSE loss in se(3).
"""
import torch
import pytorch_lightning as pl
from pytorch_lightning.callbacks import ModelCheckpoint
from torch.utils.data import DataLoader
import os

from med_rai.model import MedRAI
from med_rai.data.synthetic_se3 import SyntheticSE3Dataset

CKPT_DIR = os.environ.get("CKPT_DIR", "/content/drive/MyDrive/med_rai/checkpoints")
D_JAMBA = 4096
TRAJ_HORIZON = 10


class Phase2Module(pl.LightningModule):
    def __init__(self, phase1_ckpt: str = None, lr: float = 1e-4):
        super().__init__()
        self.save_hyperparameters()
        self.model = MedRAI()
        if phase1_ckpt:
            state = torch.load(phase1_ckpt, map_location="cpu")["state_dict"]
            self.model.load_state_dict(state, strict=False)
        self._freeze_all_but_rfm()

    def _freeze_all_but_rfm(self):
        for name, param in self.model.named_parameters():
            if not name.startswith("rfm_head"):
                param.requires_grad = False

    def training_step(self, batch, batch_idx):
        B = batch["xi_0"].shape[0]
        xi_0 = batch["xi_0"].unsqueeze(1).expand(-1, TRAJ_HORIZON, -1)  # (B, H, 6)
        xi_1 = batch["xi_1"].unsqueeze(1).expand(-1, TRAJ_HORIZON, -1)

        # Sample random timestep
        t_val = torch.rand(B, device=xi_0.device)
        t_exp = t_val.view(B, 1, 1).expand_as(xi_0)
        xi_t = (1 - t_exp) * xi_0 + t_exp * xi_1

        # Target velocity (straight line)
        u_t = xi_1 - xi_0

        # Dummy Jamba hidden states (backbone frozen — use zeros for warmup)
        h = torch.zeros(B, 1, D_JAMBA, device=xi_0.device)

        v_pred = self.model.rfm_head(xi_t, t_val, h)
        loss = ((v_pred - u_t) ** 2).mean()
        self.log("phase2/rfm_loss", loss, prog_bar=True)
        return loss

    def configure_optimizers(self):
        import bitsandbytes as bnb
        params = [p for p in self.parameters() if p.requires_grad]
        return bnb.optim.AdamW8bit(params, lr=self.hparams.lr)


def run_phase2(phase1_ckpt: str = None):
    ds = SyntheticSE3Dataset(n_samples=50000)
    loader = DataLoader(ds, batch_size=64, shuffle=True, num_workers=2)
    module = Phase2Module(phase1_ckpt=phase1_ckpt)
    ckpt_cb = ModelCheckpoint(dirpath=CKPT_DIR, filename="phase2-{epoch}-{phase2/rfm_loss:.4f}",
                              save_top_k=3, monitor="phase2/rfm_loss", every_n_train_steps=500)
    trainer = pl.Trainer(
        max_epochs=10,
        accelerator="gpu", devices=1,
        precision="bf16-mixed",
        gradient_clip_val=1.0,
        callbacks=[ckpt_cb],
    )
    trainer.fit(module, loader)
    return ckpt_cb.best_model_path


if __name__ == "__main__":
    import sys
    ckpt = sys.argv[1] if len(sys.argv) > 1 else None
    run_phase2(ckpt)
```

- [ ] **Step 2: Verify syntax**

```bash
python -c "from med_rai.training.phase2_train import Phase2Module; print('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add med_rai/training/phase2_train.py
git commit -m "feat: Phase 2 RFM head warmup on synthetic SE(3) geodesics"
```

---

## Task 14: Phase 3 Trainer — Joint Fine-tuning

**Files:**
- Create: `med_rai/training/phase3_train.py`

- [ ] **Step 1: Implement phase3_train.py**

```python
# med_rai/training/phase3_train.py
"""
Phase 3: Joint fine-tuning of all trainable components.
Trains: QLoRA on Jamba + encoders + cross_modal + all heads.
Objective: L_total = 1.0*L_rfm + 0.5*L_text + 0.3*L_gesture
"""
import pytorch_lightning as pl
from pytorch_lightning.callbacks import ModelCheckpoint, LearningRateMonitor
from pytorch_lightning.loggers import WandbLogger
from torch.utils.data import DataLoader, ConcatDataset
import os

from med_rai.model import MedRAI
from med_rai.data.jigsaws_dataset import JIGSAWSDataset
from med_rai.data.collate import medrai_collate

JIGSAWS_ROOT = os.environ.get("JIGSAWS_ROOT", "data/jigsaws")
CKPT_DIR = os.environ.get("CKPT_DIR", "/content/drive/MyDrive/med_rai/checkpoints")


def run_phase3(phase2_ckpt: str = None):
    import torch
    model = MedRAI(lr=5e-5, lambda_rfm=1.0, lambda_text=0.5, lambda_gesture=0.3)
    if phase2_ckpt:
        state = torch.load(phase2_ckpt, map_location="cpu")["state_dict"]
        model.load_state_dict(state, strict=False)

    tasks = ["Knot_Tying", "Needle_Passing", "Suturing"]
    train_ds = ConcatDataset([
        JIGSAWSDataset(root=JIGSAWS_ROOT, task=t, split="train") for t in tasks
    ])
    val_ds = ConcatDataset([
        JIGSAWSDataset(root=JIGSAWS_ROOT, task=t, split="val") for t in tasks
    ])
    train_loader = DataLoader(train_ds, batch_size=4, shuffle=True,
                              num_workers=2, collate_fn=medrai_collate)
    val_loader   = DataLoader(val_ds,   batch_size=4,
                              num_workers=2, collate_fn=medrai_collate)

    ckpt_cb = ModelCheckpoint(
        dirpath=CKPT_DIR,
        filename="phase3-{epoch}-{val/loss:.4f}",
        save_top_k=3, monitor="val/loss", every_n_train_steps=500,
    )
    lr_monitor = LearningRateMonitor(logging_interval="step")

    trainer = pl.Trainer(
        max_epochs=20,
        accelerator="gpu", devices=1,
        precision="bf16-mixed",
        gradient_clip_val=1.0,
        accumulate_grad_batches=8,      # effective batch = 32
        callbacks=[ckpt_cb, lr_monitor],
        logger=WandbLogger(project="med-rai", name="phase3") if os.environ.get("WANDB_API_KEY") else True,
    )
    trainer.fit(model, train_loader, val_loader)
    return ckpt_cb.best_model_path


if __name__ == "__main__":
    import sys
    ckpt = sys.argv[1] if len(sys.argv) > 1 else None
    run_phase3(ckpt)
```

- [ ] **Step 2: Verify syntax**

```bash
python -c "from med_rai.training.phase3_train import run_phase3; print('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add med_rai/training/phase3_train.py
git commit -m "feat: Phase 3 joint fine-tuning trainer with gradient accumulation"
```

---

## Task 15: Integration Tests

**Files:**
- Create: `med_rai/tests/test_integration.py`

- [ ] **Step 1: Write integration tests**

```python
# med_rai/tests/test_integration.py
import torch
import pytest
from unittest.mock import patch, MagicMock
from pytorch3d.transforms import random_rotations
from med_rai.model import MedRAI
from med_rai.heads.text_head import TextHead

B, SEQ_L, H = 1, 8, 10

def make_batch(device="cpu"):
    return {
        "images":         torch.randn(B, 3, 224, 224, device=device),
        "R":              random_rotations(B).to(device),
        "t":              torch.randn(B, 3, device=device) * 0.05,
        "ft":             torch.randn(B, 6, device=device),
        "token_ids":      torch.randint(0, 1000, (B, SEQ_L), device=device),
        "xi_traj":        torch.randn(B, H, 6, device=device),
        "gesture_labels": torch.randint(0, 15, (B,), device=device),
        "text_targets":   torch.randint(0, 65536, (B, SEQ_L + 3), device=device),
    }


@pytest.fixture(scope="module")
def model():
    with patch("med_rai.model.JambaBackbone") as MockBB:
        mock_bb = MagicMock()
        mock_bb.return_value = torch.randn(B, SEQ_L + 3, 4096)
        MockBB.return_value = mock_bb
        m = MedRAI()
    return m


def test_danger_signal_pathway(model):
    """
    Inject a simulated force spike: set F/T values very high.
    Patch text head to emit WARNING token.
    Verify: v_theta is zeroed and forced_gesture == RETRACT.
    """
    batch = make_batch()
    batch["ft"] = torch.ones(B, 6) * 100.0   # extreme force spike

    with patch.object(model, "_check_danger", return_value=True):
        out = model(batch)

    assert out["v_theta"].abs().sum().item() == 0.0, (
        "Trajectory was not zeroed after WARNING signal"
    )
    assert out.get("forced_gesture") == "RETRACT", (
        f"Expected RETRACT, got {out.get('forced_gesture')}"
    )


def test_full_inference_shapes(model):
    """All 4 modalities in → correct output shapes."""
    batch = make_batch()
    out = model(batch)
    assert out["v_theta"].shape == (B, H, 6)
    assert out["text_logits"].shape[0] == B
    assert out["gesture_logits"].shape == (B, 15)


def test_no_nan_in_outputs(model):
    """No NaN in any output under normal inputs."""
    batch = make_batch()
    out = model(batch)
    assert not torch.isnan(out["v_theta"]).any()
    assert not torch.isnan(out["text_logits"]).any()
    assert not torch.isnan(out["gesture_logits"]).any()


def test_trajectory_within_workspace(model):
    """Generated trajectory translations must stay within workspace after clamping."""
    from med_rai.model import WORKSPACE
    from med_rai.utils.se3_utils import se3_exp
    batch = make_batch()
    out = model(batch)
    _, t_out = se3_exp(out["v_theta"].reshape(-1, 6))
    for i, key in enumerate(["x", "y", "z"]):
        lo, hi = WORKSPACE[key]
        assert (t_out[:, i] >= lo - 1e-4).all(), f"{key} below workspace min"
        assert (t_out[:, i] <= hi + 1e-4).all(), f"{key} above workspace max"


def test_gesture_vocabulary_size(model):
    """Gesture logits must have exactly 15 classes."""
    batch = make_batch()
    out = model(batch)
    assert out["gesture_logits"].shape[-1] == 15


def test_latency(model):
    """Single inference pass must complete in < 2s (CPU)."""
    import time
    batch = make_batch()
    start = time.time()
    _ = model(batch)
    elapsed = time.time() - start
    assert elapsed < 2.0, f"Inference took {elapsed:.2f}s — too slow for CPU test"
```

- [ ] **Step 2: Run integration tests**

```bash
pytest med_rai/tests/test_integration.py -v
```

Expected: All 6 tests PASS.

- [ ] **Step 3: Run full test suite**

```bash
pytest med_rai/tests/ -v
```

Expected: All tests PASS. No failures.

- [ ] **Step 4: Commit**

```bash
git add med_rai/tests/test_integration.py
git commit -m "test: integration tests — danger override, shapes, workspace bounds, latency"
```

---

## Self-Review

### Spec coverage check

| Spec requirement | Task(s) |
|---|---|
| Endoscopic video input (Surgical ViT) | Task 3 |
| SE(3) kinematics input (log/exp + MLP) | Tasks 2, 3 |
| Force/torque input | Task 3 |
| Surgeon language input | Task 3 |
| Cross-modal alignment (cross-attention) | Task 4 |
| Jamba 4-bit NF4 QLoRA backbone | Task 5 |
| RFM Policy Head on SE(3) | Task 6 |
| Text Head (risk alerts) | Task 7 |
| Gesture Classifier (15 JIGSAWS classes) | Task 7 |
| Weighted multi-task loss | Task 8 |
| MedRAI LightningModule | Task 9 |
| Danger override (WARNING → zero trajectory + RETRACT) | Task 9, Task 15 |
| JIGSAWS dataset | Task 10 |
| Synthetic SE(3) dataset | Task 11 |
| Phase 1 encoder warmup | Task 12 |
| Phase 2 RFM warmup | Task 13 |
| Phase 3 joint fine-tuning | Task 14 |
| Workspace clamping | Tasks 2, 9 |
| 4-step Euler ODE inference | Tasks 6, 9 |
| A100 memory budget (bf16, gradient checkpoint, 8-bit AdamW) | Tasks 12–14 |

All spec requirements covered. No gaps found.

### Placeholder scan

No TBDs, TODOs, or "implement later" found. All code steps contain actual implementations.

### Type consistency

- `se3_log` / `se3_exp` defined in Task 2, used consistently in Tasks 3, 6, 9, 11
- `RFMPolicyHead.forward(xi_t, t, h)` signature defined in Task 6, called correctly in Tasks 9, 13
- `TextHead.WARNING_TOKEN_ID = 1` defined in Task 7, referenced in Task 9 `_check_danger`
- `WORKSPACE` dict defined in `model.py` Task 9, referenced in Task 15 integration test
- `TRAJ_HORIZON = 10` used consistently across Tasks 6, 9, 10, 11, 13
- `D_JAMBA = 4096` used consistently across all head initializations
